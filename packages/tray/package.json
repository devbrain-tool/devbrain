{
  "name": "devbrain-tray",
  "version": "1.0.0",
  "private": true,
  "description": "DevBrain system tray app — manages daemon lifecycle and bootstrapping",
  "author": "DevBrain",
  "homepage": "https://github.com/devbrain-tool/devbrain",
  "main": "dist/main.js",
  "scripts": {
    "build": "tsc",
    "start": "npm run build && electron dist/main.js",
    "test": "jest --config jest.config.js",
    "pack": "npm run build && electron-builder --dir",
    "dist": "npm run build && electron-builder"
  },
  "dependencies": {},
  "devDependencies": {
    "@types/node": "^24.12.2",
    "electron": "36.9.5",
    "electron-builder": "^26.0.0",
    "jest": "^30.0.0",
    "@types/jest": "^30.0.0",
    "ts-jest": "^29.3.0",
    "typescript": "~6.0.2"
  }
}
